1) before do apply local svn patches. need prepare the ENV: 
	For Example:
		$ pwd
     	 	 /media/Work/Talend_git_test
     	 	 
     	 	$ ls
		git_cmd.sh       tbd-studio-se      tdq-studio-ee   tmdm-server-se
		--patches        tcommon-studio-ee  tdq-studio-se   tmdm-studio-ee
		reset_branch.sh  tcommon-studio-se  tesb-studio-ee  tmdm-studio-se
		tamc-ee          tdi-studio-ee      tesb-studio-se  toem-studio-ee
		tbd-studio-ee    tdi-studio-se      tmdm-server-ee  toem-studio-se
		
	PS, - git_cmd.sh 
		just for doing git command for each git repositories. for example:
			$ ./git_cmd.sh co master
		 (will switch all git repositories to master).
		 
	    - reset_branch.sh see point 2

2) before do apply the patch, maybe need do reset in git root path, if existed the old one:
     for example: 
     	$ pwd
     	  /media/Work/Talend_git_test
     	
	$ ./reset_branches.sh patch/v5.4.1
	$ ./reset_branches.sh patch/v5.3.2
	$ ./reset_branches.sh patch/v5.3.1

3) do apply the patches(befault, all patches put in "--patches" path of git root path):
	v5.4.1: 
		$ pwd 
		  /media/Work/Talend_git_test/----patches/v5.4.1
		  
		$ ./patch541.sh
		
	v5.3.2: only need create branches, "./patch532.sh"
		$ pwd 
		  /media/Work/Talend_git_test/----patches/v5.3.2
		  
		$ ./patch532.sh
		
	v5.3.1: 
		$ pwd 
		  /media/Work/Talend_git_test/----patches/v5.3.1
		  
		$ ./patch531.sh
		
4) git push branch
     	$ pwd
     	  /media/Work/Talend_git_test
     	  
	$ git push origin patch/v5.4.1
	$ git push origin patch/v5.3.2
	$ git push origin patch/v5.3.1
